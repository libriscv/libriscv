## CoreMark benchmarking example

This example project shows how I'm running CoreMark.

It will get automatically downloaded and the CLI loads and executes it with an execution timeout enabled. In other words, we aren't cheating and disabling features that would otherwise have enabled in the wild.
